/*var x, y, from, reachable;
x           = argument0; // new node's x
y           = argument1; // new node's y
from        = argument2; // in which direction was the previous node?
reachable   = argument3; // is the previous node reachabale from the new node?

newNode = instance_create(x,y,PathNode);
switch(from) {
    case NODE_LEFT:
        global.lastNode.
        break;
    case NODE_RIGHT:
        break;
    case NODE_TOPLEFT:
        break;
    case NODE_TOPRIGHT:
        break;
}
if(reachable) {
    newNode.node[from] = global.lastNode;
}
newNode
global.lastNode = newNode;*/
